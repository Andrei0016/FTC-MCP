package <<PACKAGE>>.core.vars;

/** Per-loop shared bus between subsystems. Robot fills it before calling update(). */
public class SubsystemData {
    // Add cross-subsystem fields here (poses, targets, sensor snapshots, ...).
}
